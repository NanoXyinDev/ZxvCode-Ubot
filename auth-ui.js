const form=document.getElementById('form'),msg=document.getElementById('msg');
form?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);const path=location.pathname.includes('register')?'register':'login';if(path==='register'&&fd.get('password')!==fd.get('confirm')){msg.textContent='❌ Password tidak sama.';return}msg.textContent='⏳ Memproses...';try{const r=await fetch('/api/auth?action='+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:fd.get('username'),password:fd.get('password'),email:fd.get('email')||''})});const d=await r.json();if(!r.ok||!d.ok)throw Error(d.error||'Request gagal');location.href='/'}catch(err){msg.textContent='❌ '+err.message}});
