# Vercel
1. Import this folder as the project root.
2. Framework preset: Other.
3. Build command: none.
4. Output directory: `.`.
5. Deploy.

The `/connect` page is UI-only. There is intentionally no `/api/bridge` dependency.
