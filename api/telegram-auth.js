export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({ok:false,error:'Method tidak diizinkan'});const base=(process.env.BOT_BRIDGE_URL||'').replace(/\/$/,'');if(!base)return res.status(500).json({ok:false,error:'BOT_BRIDGE_URL belum diset.'});const action=String(req.query?.action||'');const allowed=new Set(['request-code','verify-code','verify-2fa']);if(!allowed.has(action))return res.status(400).json({ok:false,error:'Action tidak dikenal.'});try{const r=await fetch(base+'/telegram/'+action,{method:'POST',headers:{'Content-Type':'application/json','X-Bridge-Key':process.env.BOT_BRIDGE_SECRET||''},body:JSON.stringify(req.body||{})});const text=await r.text();res.status(r.status).setHeader('Content-Type','application/json');return res.send(text)}catch{return res.status(502).json({ok:false,error:'Bridge Telegram tidak dapat dihubungi.'})}}